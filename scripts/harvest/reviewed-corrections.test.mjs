import test from 'node:test';import assert from 'node:assert/strict';
import {applyReviewedCorrection} from './reviewed-corrections.mjs';import{effectiveStatus}from'./domain.mjs';
const r={id:'11291c30724438736a8d',url:'https://jobs.ethz.ch/job/view/11996',title:'Doctoral Position at the Professorship of Marketing & Technology (TUM)',institution:'ETH Zurich',country:'CH',stage:'faculty',status:'listed',verifiedAt:'2026-09-21',evidence:{contentHash:'original-page'}};
const text='This job announcement is for position B. TUM Campus Munich. 25th August 2026. TV-L E13 at TUM.';
test('a hosting portal does not determine the reviewed employer, stage or closing date',()=>{const corrected=applyReviewedCorrection(r,text);assert.equal(corrected.institution,'Technical University of Munich');assert.equal(corrected.country,'DE');assert.equal(corrected.stage,'doctorado');assert.equal(effectiveStatus(corrected,Date.parse('2026-09-21')),'closed');assert.equal(corrected.evidence.contentHash,'original-page');assert.equal(r.institution,'ETH Zurich');});
test('changed correction evidence requires review instead of silently overwriting facts',()=>{assert.throws(()=>applyReviewedCorrection(r,text.replace('25th August 2026','25th August 2027')),/evidence_changed/);assert.throws(()=>applyReviewedCorrection({...r,title:'Different job'},text),/evidence_changed/);assert.deepEqual(applyReviewedCorrection({id:'unrelated'},''),{id:'unrelated'});});

'use client';
import Link from './site-link';
import {X} from 'lucide-react';
import {Dialog,DialogContent,DialogTitle,DialogDescription,DialogClose} from './ui/dialog';
import {Table,TableHeader,TableBody,TableRow,TableHead,TableCell} from './ui/table';
import {COUNTRY_NAMES,dateLabel,type Opportunity} from '@/lib/types';
export default function Comparison({selected,onClose}:{selected:Opportunity[];onClose:()=>void}){return <Dialog open onOpenChange={open=>{if(!open)onClose();}}><DialogContent className="comparison-dialog" showCloseButton={false}><DialogClose className="dialog-close" aria-label="Cerrar comparación"><X/></DialogClose><DialogTitle>Las condiciones, una junto a otra.</DialogTitle><DialogDescription>Los importes conservan su moneda y periodo originales; bruto y neto no son equivalentes.</DialogDescription><p className="mobile-table-hint">Desliza la tabla para comparar todas las columnas.</p><Table><TableHeader><TableRow><TableHead>Condición</TableHead>{selected.map(r=><TableHead key={r.id}>{r.institution}<Link href={'/oportunidad/'+r.id}>{r.title}</Link></TableHead>)}</TableRow></TableHeader><TableBody>{[['País',(r:Opportunity)=>COUNTRY_NAMES[r.country]],['Nivel de entrada',(r:Opportunity)=>r.entry],['Financiación',(r:Opportunity)=>r.funding.text],['Duración',(r:Opportunity)=>r.duration||'Por confirmar'],['Contrato',(r:Opportunity)=>r.contract||'No confirmado'],['Matrícula',(r:Opportunity)=>r.feeNote||'Consultar la fuente'],['Cierre',(r:Opportunity)=>dateLabel(r.deadline)],['Comprobado',(r:Opportunity)=>dateLabel(r.verifiedAt)]].map(([name,value])=><TableRow key={String(name)}><TableCell>{String(name)}</TableCell>{selected.map(r=><TableCell key={r.id}>{(value as (r:Opportunity)=>string)(r)}</TableCell>)}</TableRow>)}</TableBody></Table></DialogContent></Dialog>;}
